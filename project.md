# Python+Tkinter 微服务启动器开发指导文档
## 一、需求概述
你需要基于 Python + Tkinter 开发一款图形化（GUI）微服务启动器工具，核心目标是对多个通过 .bat 脚本启动的微服务进行全生命周期管理，具体需实现：bat 脚本启动 / 停止、进程实时检查、日志收集与展示、异常自动重启、自定义健康监测、按健康状态顺序启动 六大核心能力，工具需具备易用性、稳定性，且界面直观友好。
## 二、技术栈与依赖
1. 核心技术
编程语言：Python 3.12+（兼容主流版本）
GUI 框架：Tkinter（Python 内置，无需额外安装）
进程管理：subprocess（启动 / 管理 bat 进程）、psutil（进程状态检查、PID 监控）
异步 / 定时任务：threading（避免阻塞 GUI 主线程）、time（定时检测）
日志处理：Python 内置 logging 模块（文件日志）+ Tkinter 文本框（实时展示）
配置管理：JSON 文件（存储微服务配置，如 bat 路径、健康检查规则、启动顺序等）
健康检测扩展：可选 requests（HTTP 接口检测）、socket（端口检测）
2. 安装包
- 完整可运行的 Python 可执行程序（.exe 文件）；
- 示例 services_config.json 配置文件；
- 简单的使用说明（如配置修改、启动工具的命令）；

## 三、核心功能拆解与实现要求
1. 基础 UI 框架设计
布局要求
区域	功能描述
左侧服务列表	展示所有微服务名称、健康状态（颜色区分：绿色 = 健康、黄色 = 启动中、红色 = 异常、灰色 = 停止）、启动顺序、操作按钮（单个启动 / 停止 / 重启）
右侧日志面板	分服务展示日志（支持切换服务、清空日志、导出日志），实时刷新输出
底部操作栏	批量操作按钮（启动全部、停止全部、重启全部）、配置刷新按钮、日志级别筛选
交互要求
服务状态实时更新（每秒刷新一次）；
Tkinter 界面更新需使用 after() 方法（避免线程安全问题）；
操作按钮点击后有状态反馈（如加载提示、执行结果弹窗）；
支持拖拽 / 手动修改微服务启动顺序。
2. 核心功能模块实现规范
模块 1：配置管理（JSON 配置文件）
配置文件路径：工具根目录 services_config.json；
配置项格式（每个微服务一个节点）：
``` json
{
  "services": [
    {
      "service_name": "基础服务A",
      "command": "D:/microservice/A/start.bat",
      "working_dir": "D:/microservice/A",
      "start_order": 1,          // 启动顺序（数字越小越先启动）
      "health_check_type": "port",// 健康检查类型：port/http/none
      "health_check_config": {   // 健康检查配置（按需定义）
        "host": "127.0.0.1",         // 目标主机
        "port": 8080,                // 目标端口
        "retries": 30,               // 失败重试次数（宽限期后计数）
        "interval": 1,               // 检查间隔（秒，支持小数）
        "start_period": 5            // 启动宽限期（秒，期间失败不计数）
      },
      "environment": {          // 环境变量
        "PORT": "8080",
        "DEBUG": "1"
      },
      "restart": "always",       // 自动重启策略：always/unless-stopped/on-failure
      "max_restart_times": 5,    // 最大重启次数（0=无限）
      "restart_interval": 3      // 重启间隔（秒）
    },
    {
      "service_name": "业务服务B",
      "command": "D:/microservice/B/start.bat",
      "working_dir": "D:/microservice/B",
      "start_order": 2,
      "health_check_type": "http",
      "health_check_config": {
        "url": "http://127.0.0.1:8081/health",
        "expected_code": 200,
        "retries": 20,
        "interval": 1,
        "start_period": 5
      },
      "environment": [
        "API_URL=http://127.0.0.1:8081",
        "DEBUG=1"
      ],
      "restart": "on-failure",
      "max_restart_times": 3,
      "restart_interval": 5
    }
  ]
}
```

模块 2：Command 命令启动 / 停止
启动命令：使用 subprocess.Popen 执行命令，必须指定 creationflags=subprocess.CREATE_NEW_PROCESS_GROUP（避免杀死子进程时误杀主程序），记录启动的进程 PID；
停止命令关联进程：通过 psutil 根据 PID 找到所有子进程，递归终止（避免只杀父进程、子进程残留）；
异常处理：捕获命令执行失败等异常，记录到日志并弹窗提示。
模块 3：进程检查
检查频率：每秒执行一次进程状态检测；
检查逻辑：
根据 PID 调用 psutil.pid_exists() 判断进程是否存活；
若进程不存在，标记服务状态为「异常」，并触发自动重启逻辑；
若进程存在但健康检查失败，标记为「异常」，仅记录不触发重启（区分「进程存活但服务不可用」和「进程直接退出」）；
线程安全：进程检查放在独立线程中执行，结果通过 Tkinter after() 方法更新到界面。
模块 4：日志管理
日志收集：
启动 bat 时，重定向 stdout/stderr 到管道，实时读取并写入日志文件；
按服务分类存储日志文件（按日期切割，避免单文件过大）；
日志展示：
Tkinter 文本框支持「按服务筛选」「清空当前日志」「导出日志（保存为 txt）」；
日志输出带时间戳、服务名称、日志级别（INFO/ERROR/WARN）；
性能优化：日志写入使用缓冲区，避免高频 IO 阻塞；界面日志仅保留最近 1000 行，超出自动清理。
模块 5：自动重启
触发条件：进程检测到服务退出（PID 不存在）且 `restart` 策略允许（`always`/`unless-stopped`/`on-failure`）；
限制逻辑：
累计重启次数达到 max_restart_times 时，停止自动重启并弹窗告警；
两次重启间隔不小于 restart_interval 秒，避免高频重启；
状态记录：记录每次重启的时间、原因，写入日志文件。
模块 6：健康监测
支持两种核心检测方式（可扩展）：
端口检测（port）：通过 socket 连接指定 host:port，能连接则判定健康；
HTTP 接口检测（http）：调用指定 URL，返回码匹配 expected_code 则判定健康；
无检测（none）：仅通过进程是否存活判定健康；
检测频率：监控循环每 1 秒执行一次健康检查；顺序启动阶段按 `health_check_config.interval` 控制检查间隔。
结果处理：检查失败时标记服务状态为「异常」，并在日志中记录失败原因（如「端口 8080 连接超时」「HTTP 接口返回 500」）。
模块 7：按健康状态顺序启动
核心逻辑：
读取配置中的 start_order，按数字升序排序微服务；
先启动第 1 个服务，持续检测其健康状态；
第 1 个服务「健康」后，再启动第 2 个服务，以此类推；
若某服务启动失败 / 健康检查失败，暂停后续服务启动，弹窗提示并记录日志；
手动干预：支持「跳过失败服务继续启动」「终止顺序启动」等手动操作。
## 四、代码开发规范
1. 结构设计（模块化）
python
运行
| 类名               | 职责说明 |
|--------------------|----------|
| ServiceConfig      | 服务配置解析类（读取/解析 JSON 配置） |
| ProcessManager     | 进程管理类（启动/停止/检查 bat 进程） |
| HealthChecker      | 健康检查类（端口/HTTP 检测） |
| LogManager         | 日志管理类（文件存储+界面展示） |
| ServiceLauncherGUI | GUI 主类（Tkinter 界面+交互逻辑） |
| SequentialStarter  | 顺序启动类（按健康状态控制启动流程） |
2. 关键约束
Tkinter 界面更新：所有 UI 操作（如文本框插入、按钮状态修改）必须在主线程执行，子线程通过 root.after(0, callback) 触发更新；
异常处理：所有 IO 操作（文件读写、进程启动、网络请求）必须加 try-except 捕获异常，避免工具崩溃；
资源释放：工具关闭时，先终止所有微服务进程，再释放文件句柄、线程等资源；
命名规范：变量 / 函数使用下划线命名（如 start_service），类使用大驼峰命名（如 ServiceManager），常量全大写（如 MAX_RESTART_TIMES）。
## 五、测试与验证要点
基础功能：
单个服务启动 / 停止是否正常，bat 脚本能否正确执行；
进程检查是否准确（杀死进程后，工具能否立即识别）；
自动重启：
手动终止服务进程，工具是否按配置重启，重启次数 / 间隔是否符合要求；
健康监测：
关闭服务端口 / 停掉 HTTP 接口，工具能否标记为异常；恢复后能否自动更新状态；
顺序启动：
配置 2 个以上服务，验证是否先启动顺序 1 的服务，健康后再启动顺序 2 的服务；
故意让顺序 1 的服务启动失败，验证是否暂停后续启动；
日志管理：
日志是否实时写入文件，界面能否正确展示，导出功能是否可用；
稳定性：
长时间运行（1 小时以上）是否内存泄漏、界面无响应；
批量启动 / 停止 5+ 个服务，是否出现进程残留、日志错乱。
## 六、交付物要求
完整可运行的 Python 代码（含所有依赖类 / 函数）；
完整可运行的 Python 可执行程序（.exe 文件）；
示例 services_config.json 配置文件；
简单的使用说明（如配置修改、启动工具的命令）；
代码注释覆盖率 ≥ 80%（核心函数 / 类必须有详细注释）。
## 总结
### 核心开发要点
进程管理是核心：需通过 subprocess + psutil 精准控制 bat 进程，避免残留；
线程安全是关键：Tkinter 主线程不可阻塞，所有耗时操作（进程检查、健康检测）需放在子线程；
配置驱动设计：微服务的所有规则（启动顺序、健康检查、重启策略）通过 JSON 配置，避免硬编码；
异常容错：需覆盖进程启动失败、健康检查超时、日志写入失败等场景，保证工具稳定性。
### 功能优先级
基础：bat 启动 / 停止 + 进程检查 + 日志展示；
核心：健康监测 + 顺序启动；
增强：自动重启 + 日志导出 + 批量操作。
